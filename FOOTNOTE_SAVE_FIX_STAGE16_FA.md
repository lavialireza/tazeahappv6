# Tazieh Android — Footnote Save Fix Stage 16

مبنای این نسخه: Stage 15 (Viewer Add Image Fixed).

## تغییر واقعی
- ذخیره پاورقی در صفحه متن در هر دو Build Admin و Viewer با بررسی صریح ورودی انجام می‌شود.
- برای رکورد جدید `uid` به‌صورت صریح و یکتا تولید می‌شود.
- پس از INSERT، رکورد دوباره از پایگاه داده خوانده و وجود آن بررسی می‌شود؛ بنابراین ذخیره موفق/ناموفق به‌صورت خاموش رها نمی‌شود.
- عملیات INSERT و بررسی آن در Dispatcher مربوط به I/O انجام می‌شود.
- مسیر ویرایش/حذف پاورقی موجود حفظ شده است.
- بخش‌های دیگر برنامه تغییر داده نشده‌اند.

## Build
در این محیط Android SDK/Gradle Wrapper قابل اجرای محلی در دسترس نیست؛ بنابراین APK Build‌شده ادعا نمی‌شود. ساختار ZIP و تغییرات سورس بررسی شده‌اند.

# Tazieh Android — Footnote Stage 19

این نسخه بر پایه Stage15/Stage18 ساخته شده و مشکل واقعی «ذخیره پاورقی در دسترس نیست» در مسیر صفحه متن را اصلاح می‌کند.

## اصلاح واقعی
- `TextPagerScreen` اکنون پاورقی‌های هر بخش را از `FootnoteDao` دریافت می‌کند.
- عملیات افزودن، ویرایش و حذف پاورقی به همان `sectionId` صفحه متن متصل شده است.
- پیام پیش‌فرض «ذخیره پاورقی در دسترس نیست» دیگر در مسیر واقعی صفحه متن استفاده نمی‌شود.
- پس از افزودن/ویرایش/حذف، فهرست پاورقی همان بخش دوباره از پایگاه داده خوانده می‌شود.
- سایر قابلیت‌ها از جمله گالری و بزرگنمایی تصویر تغییر داده نشده‌اند.

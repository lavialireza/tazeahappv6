# Stage 52 — تکمیل مدیریت دسترسی Viewer

مبنای این نسخه: Tazieh Android ACCESS Stage51.1 Build Fix.

## تغییرات واقعی
1. وضعیت ذخیره‌نشده برای پروفایل عمومی و مجوزهای کاربر خاص نمایش داده می‌شود.
2. برای هر کاربر خاص، آخرین تغییر، آخرین ارسال و آخرین اعمال سیاست و وضعیت همگام‌سازی نمایش داده می‌شود.
3. گروه‌های مجوز، حالت باز/بسته مستقل دارند و دیگر یک متغیر بازشدن مشترک ندارند.
4. تغییرات حساس شامل copy، share، pdf، gallery و training قبل از اعمال با تأیید کاربر انجام می‌شود.
5. غیرفعال‌کردن گروهی مجوزها نیز در صورت شامل‌بودن قابلیت حساس، با تأیید جداگانه انجام می‌شود.
6. دکمه «اعمال تغییرات و ارسال» برای کاربر خاص اضافه شده و پس از ذخیره واقعی، سیاست را برای همان Viewer ارسال می‌کند.
7. پس از ارسال مستقیم موفق، زمان/اثر انگشت/نسخه ارسال در سیاست کاربر ثبت و فهرست دوباره بارگذاری می‌شود. در Share Sheet صرفاً باز شدن پنجره، ارسال موفق فرض نمی‌شود.
8. وضعیت مجوزهای فعال و نیاز به همگام‌سازی در کارت کاربران خاص نیز نمایش داده می‌شود.
9. اطلاعات مربوط به dictionary، footnoteSync و calendar همچنان بدون افزودن Permission جدید باقی مانده‌اند؛ این تصمیم برای جلوگیری از ایجاد دسترسی صوری یا ناقص حفظ شده است.

## کنترل صحت
- فایل اصلی اصلاح‌شده: `app/src/main/java/com/example/bookapp/ui/screens/ViewerAccessManagementScreen.kt`
- تعداد کل Permissionهای موجود در Policy تغییر نکرده و همان 15 مورد Stage51.1 باقی مانده است.
- تغییر `flatMap` به `mapNotNull` مرحله 51.1 نیز در این مبنا حفظ شده است.
- ZIP پس از ساخت با `unzip -t` بررسی می‌شود.
- در این محیط Android SDK/Gradle Wrapper قابل استفاده برای اجرای Build کامل موجود نیست؛ بنابراین موفقیت Android Build ادعا نمی‌شود.

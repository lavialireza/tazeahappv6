# مرحله ۲ — مدیریت دسترسی کامل Admin → Viewer

این مرحله سیستم سیاست دسترسی را از حالت «ذخیره محلی در Admin» به یک سیستم عملیاتی تبدیل می‌کند.

## امکانات
- Admin می‌تواند برای پروفایل عمومی تمام قابلیت‌ها را روشن/خاموش کند.
- Admin می‌تواند برای یک Viewer مشخص بر اساس Installation ID سیاست اختصاصی بسازد.
- Admin می‌تواند سیاست عمومی یا سیاست یک Viewer خاص را به فایل JSON صادر کند.
- فایل سیاست دارای HMAC-SHA256 برای تشخیص دستکاری است.
- Viewer در تنظیمات گزینه «دریافت سیاست دسترسی» دارد.
- Viewer فقط سیاستی را می‌پذیرد که برای شناسه خودش یا سیاست عمومی صادر شده باشد.
- سیاست منقضی‌شده پذیرفته نمی‌شود.
- پس از وارد کردن سیاست، قابلیت‌ها بدون نصب مجدد APK اعمال می‌شوند.
- دسترسی مؤثر Viewer پس از راه‌اندازی مجدد نیز حفظ می‌شود.

## قابلیت‌های قابل کنترل
- مطالعه
- جستجو
- جستجوی پیشرفته
- مقایسه
- تمرین
- صوت
- تبدیل متن به گفتار
- یادداشت
- علاقه‌مندی
- گالری تصاویر
- کپی متن
- اشتراک‌گذاری
- PDF

## تفکیک Viewer
در Viewer قابلیت‌های مدیریتی محتوا همچنان وجود ندارند و فقط امکانات مطالعاتی بر اساس سیاست دسترسی نمایش داده می‌شوند.
افزودن/حذف/ویرایش تصاویر و خروجی‌های مدیریتی نیز در حالت Viewer محدود شده‌اند.

## انتقال سیاست
1. در Admin وارد «مدیریت دسترسی Viewer» شوید.
2. مجوزها را با Switch تنظیم کنید.
3. برای سیاست عمومی یا Viewer خاص، «خروجی سیاست» را بزنید.
4. فایل JSON را به گوشی Viewer منتقل کنید.
5. در Viewer → تنظیمات → مجوزهای Viewer → «دریافت سیاست دسترسی» فایل را انتخاب کنید.
6. سیاست بلافاصله اعمال می‌شود.

### نکته امنیتی
این مکانیزم برای کنترل قابلیت‌ها و جلوگیری از تغییر ساده فایل سیاست طراحی شده است؛ مانند هر نرم‌افزار آفلاین، حفاظت مطلق در برابر مهندسی معکوس ممکن نیست.

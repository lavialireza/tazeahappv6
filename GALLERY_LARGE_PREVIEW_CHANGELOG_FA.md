# اصلاح بزرگنمایی تصاویر گالری

مبنای این نسخه دقیقاً فایل `Tazieh_Android_GALLERY_ONLY_STAGE12_REAL_IMPLEMENTATION` ارسالی کاربر است.

تغییرات فقط در قابلیت نمایش گالری انجام شده‌اند:

- با لمس تصویر بندانگشتی، همان تصویر در یک نمای بزرگ باز می‌شود.
- نمای بزرگ تقریباً تمام صفحه را در اختیار تصویر قرار می‌دهد و نسبت تصویر حفظ می‌شود.
- دکمه بستن در گوشه تصویر اضافه شده است.
- با لمس بیرون/بازگشت نیز نمای بزرگ بسته می‌شود.
- اندازه بندانگشتی از ۱۴۰dp به ۱۸۰dp افزایش یافته است.
- مسیر ورود به گالری، مدیریت عکس، حذف عکس و توضیحات تغییر داده نشده است.
- در گالری تجمیعی تصاویر نیز همین پیش‌نمایش بزرگ فعال شده است.
- import مربوط به `Close` در `AllImagesGalleryScreen.kt` به‌صورت صریح اضافه شده تا خطای Build گزارش‌شده (`Unresolved reference: Close`) ایجاد نشود.

این نسخه در محیط حاضر APK با Gradle ساخته نشده است؛ بنابراین موفقیت Build نهایی ادعا نمی‌شود. ZIP از نظر ساختار و محتویات تست شده است.

# Stage 78 — قفل نهایی مسیرهای ذخیره محتوای اصلی

این مرحله ادامه مستقیم Stage 77 است و یک لایه دفاعی پایین‌تر از UI اضافه می‌کند.

## تغییر واقعی
در نسخه User/Viewer، مسیرهای مستقیم ذخیره‌سازی برای:
- دیکشنری سفارشی
- اصلاحات متن

اکنون قبل از نوشتن از `ViewerContentWriteGuard` عبور می‌کنند و در Viewer متوقف می‌شوند.

بنابراین حتی اگر دکمه UI، سیاست دسترسی یا مسیر فراخوانی اشتباه باشد، این دو مسیر ذخیره‌سازی نمی‌توانند محتوای اصلی را در نسخه عمومی تغییر دهند.

## Admin
در flavor مدیر هیچ محدودیتی اضافه نشده و مسیرهای مدیریت محتوا مانند قبل فعال هستند.

## تست
یک تست قراردادی برای guard اضافه شده است.

## Build
پس از Stage 77 موفق، با همان GitHub Actions:
- `assembleAdminDebug`
- `assembleViewerDebug`

را اجرا کنید.

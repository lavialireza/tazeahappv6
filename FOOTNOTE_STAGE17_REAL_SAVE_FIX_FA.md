# Tazieh Android — Footnote Stage 17

این نسخه بر پایه مستقیم Stage15 ساخته شده است و فقط ذخیره پاورقی در صفحه متن را اصلاح می‌کند.

- افزودن پاورقی اکنون به صورت suspend انجام می‌شود و پنجره فقط بعد از موفقیت واقعی بسته می‌شود.
- برای هر پاورقی UID یکتا ایجاد می‌شود.
- بعد از INSERT، رکورد با همان UID از Room دوباره خوانده و ذخیره واقعی آن تأیید می‌شود.
- خطای ذخیره در خود پنجره پاورقی نمایش داده می‌شود.
- دکمه ذخیره هنگام ذخیره غیرفعال و وضعیت «در حال ذخیره…» نمایش داده می‌شود.
- Admin و Viewer از همان مسیر TextScreen/AppNavigation استفاده می‌کنند، بنابراین اصلاح برای هر دو flavor اعمال شده است.
- سایر قابلیت‌ها، از جمله گالری و بزرگنمایی تصویر، دست‌نخورده باقی مانده‌اند.

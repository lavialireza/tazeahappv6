# Stage54.1 — دسترسی‌های والد/فرزند و اعمال جزئی

این نسخه بر پایه Stage53.2 ساخته شده است.

## تغییرات واقعی
- مدل Permission به والد/فرزند توسعه یافت و `permissionParents` اضافه شد.
- برای نسخه‌های قبلی، مجوزهای فرزند در صورت نبودن در سیاست، وضعیت والد را به ارث می‌برند.
- `featureEnabled` در Viewer اکنون وضعیت والد را نیز بررسی می‌کند؛ خاموش بودن والد، فرزند را مؤثرًا خاموش می‌کند.
- پاورقی به فرزندان مستقل تقسیم شد: مشاهده، افزودن، ویرایش، حذف و همگام‌سازی با دیکشنری.
- دکمه‌ها و عملیات واقعی پاورقی به مجوز فرزند مربوط متصل شدند.
- همگام‌سازی پاورقی با کلید مستقل `footnoteSync.dictionary` کنترل می‌شود.
- مدیریت دسترسی Admin به شکل درختی والد/فرزند نمایش داده می‌شود و والد می‌تواند فرزندان را یکجا فعال/غیرفعال کند.
- فرزندان مستقل نیز قابل تنظیم هستند.
- سیاست انتقال برای کلیدهای جدید نیز صادر و دریافت می‌شود.

## وضعیت Build
در این محیط Android SDK/Gradle Wrapper قابل اجرا در دسترس نبود؛ بنابراین Build موفق ادعا نمی‌شود. ساختار ZIP و توازن نحوی فایل‌های Kotlin بررسی شده است.

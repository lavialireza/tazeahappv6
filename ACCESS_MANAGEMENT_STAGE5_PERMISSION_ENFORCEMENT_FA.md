# ACCESS MANAGEMENT — Stage 5

## هدف
اجرای مجوزهای Viewer فقط در منوی اصلی نباشد و در مسیرهای واقعی استفاده از قابلیت‌ها نیز اعمال شود.

## اصلاحات
- جستجو: اگر مجوز علاقه‌مندی خاموش باشد، دکمه علاقه‌مندی کنار نتایج نمایش داده نمی‌شود.
- متن: دکمه علاقه‌مندی فقط در صورت داشتن مجوز bookmarks فعال است.
- متن: پخش صوت واقعی فقط با مجوز audio انجام می‌شود.
- متن: تبدیل متن به گفتار فقط با مجوز tts انجام می‌شود.
- Viewer عمومی: گزینه‌های افزودن/تعویض/حذف فایل صوتی که مخصوص مدیریت محتوا هستند نمایش داده نمی‌شوند.
- مسیرهای ورود به جستجو، علاقه‌مندی، یادداشت، گالری و نقش من با سیاست دسترسی کنترل می‌شوند.
- اگر مسیر غیرمستقیم بخواهد قابلیت غیرفعال را باز کند، به‌جای اجرای آن پیام عدم دسترسی نمایش داده می‌شود.
- منطق قبلی Admin بدون محدودیت باقی می‌ماند.

## نکته
این مرحله بر پایه Stage 4 Permission Refresh ساخته شده و شماره نسخه به‌تنهایی تغییر نکرده است؛ کدهای مربوط به enforcement واقعاً تغییر کرده‌اند.

## تست پیشنهادی
در Viewer به‌صورت جداگانه مجوزهای `bookmarks`, `notes`, `gallery`, `audio`, `tts`, `search` را خاموش/روشن کرده و دریافت سیاست را انجام دهید. سپس از منوی اصلی، جستجو و صفحه متن بررسی کنید که هر قابلیت دقیقاً مطابق سیاست نمایش داده یا مسدود شود.

# Gallery Viewer Large Preview – Stage 14

این نسخه مستقیماً بر پایه Stage 12 ساخته شده است.

تغییر این مرحله فقط مربوط به نمایش تصویر در گالری است:
- در گالری تعزیه، با لمس تصویر بندانگشتی، تصویر در نمای بزرگ/تمام‌صفحه باز می‌شود.
- در گالری تجمیعی نیز همین قابلیت فعال است.
- نسبت تصویر با `ContentScale.Fit` حفظ می‌شود.
- بستن با دکمه بستن یا Back انجام می‌شود.
- در حالت Viewer، قابلیت فقط نمایشی است و افزودن/حذف تصویر فعال نمی‌شود.
- مسیرهای ورود به گالری، دسترسی‌ها و ذخیره تصاویر تغییر داده نشده‌اند.
- خطای `Close` نیز با import صحیح اصلاح شده است.

تست ZIP: انجام شد.
Build محلی Android: در این محیط SDK/Gradle Android در دسترس نیست؛ بنابراین APK ساخته‌شده ادعا نمی‌شود.

# Stage 41 — سخت‌گیری امنیتی دسترسی Viewer

این مرحله بر مبنای Stage40 ساخته شده و فقط منطق امنیتی دسترسی را تکمیل می‌کند.

## تغییرات واقعی
- اگر دستگاه دارای رکورد کاربر خاص باشد و آن کاربر غیرفعال شده یا تاریخ دسترسی‌اش گذشته باشد، Viewer دیگر به مجوزهای عمومی fallback نمی‌کند و مجوز مؤثر خالی دریافت می‌کند.
- شناسه مقصد هنگام ساخت/دریافت سیاست نرمال‌سازی می‌شود تا تفاوت حروف کوچک/بزرگ باعث خطای تطبیق نشود.
- سیاست با شناسه دستگاه دیگر همچنان رد می‌شود.
- نسخه سیاست باید معتبر و بزرگ‌تر از آخرین نسخه دریافت‌شده باشد؛ بنابراین یک فایل معتبرِ قدیمی نمی‌تواند سیاست جدیدتر را overwrite کند (جلوگیری از replay).
- پس از اعمال موفق سیاست، آخرین نسخه دریافت‌شده به‌صورت پایدار ثبت می‌شود.

## Build
Android SDK/Gradle در محیط توسعه این بسته در دسترس نبود؛ بنابراین APK محلی ادعا نشده است. این بسته برای Build در GitHub Actions آماده شده است.

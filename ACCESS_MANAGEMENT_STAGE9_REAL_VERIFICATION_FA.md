# ACCESS MANAGEMENT Stage 9 — اصلاحات واقعی پس از بازبینی Stage 8

این نسخه پس از گزارش کاربر که اصلاحات در APK قابل مشاهده نبوده، کدهای مرتبط را دوباره بررسی و تقویت می‌کند.

- پاورقی: ذخیره/ویرایش/حذف با خطای قابل مشاهده در UI در صورت شکست؛ مقدارها trim می‌شوند و پس از عملیات دوباره از Room خوانده می‌شوند.
- کاربران خاص: پس از ذخیره فرم پاک نمی‌شود؛ رکورد ذخیره‌شده دوباره خوانده و در همان صفحه نشان داده می‌شود تا «ذخیره نشدن» با ناپدیدشدن فرم اشتباه نشود. ذخیره SharedPreferences با commit و راستی‌آزمایی باقی است.
- گالری: در صفحه فهرست هر تعزیه، کارت واضح «گالری تصاویر این مجلس» وجود دارد و در صفحه گالری، دکمه واقعی «افزودن عکس» از حافظه/گالری گوشی فعال است.
- معرفی برنامه: لینک مخزن GitHub در متن معرفی عمومی وجود ندارد؛ آدرس سایت واقعی فقط در صورت تنظیم APP_WEBSITE نمایش داده می‌شود.
- مجوزها: footnotes و appIntro در ViewerAccessPolicy هستند و gallery نیز مستقل کنترل می‌شود.

## محدودیت تست
در این محیط Android SDK/Gradle کامل برای ساخت APK موجود نیست؛ بنابراین این ZIP از نظر فایل و کد بررسی شده اما ادعای Build موفق APK محلی نمی‌شود.

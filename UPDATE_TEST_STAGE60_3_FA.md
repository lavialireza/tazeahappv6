# Stage60.3 — تست واقعی بروزرسانی آنلاین

این مرحله برای آزمایش زنجیره واقعی بروزرسانی است. برنامه در حالت عادی آفلاین می‌ماند و فقط هنگام انتخاب «بروزرسانی برنامه» به اینترنت وصل می‌شود.

## روش تست
1. یک Build از Workflow بگیرید و APK موردنظر را روی گوشی نصب کنید. شماره `versionCode` این Build برابر شماره اجرای GitHub Actions است.
2. یک تغییر کوچک و غیرمرتبط با منطق برنامه انجام دهید (مثلاً تغییر متن Release Notes) و دوباره Workflow را اجرا کنید. اجرای دوم یک `versionCode` بالاتر خواهد داشت.
3. APK اجرای دوم را نصب نکنید. گوشی همچنان نسخه اجرای اول را داشته باشد.
4. در برنامه بروید به: `تنظیمات → بروزرسانی برنامه`.
5. روی «بررسی بروزرسانی» بزنید.
6. برنامه Release رسمی را بررسی می‌کند؛ اگر `versionCode` جدید بزرگ‌تر باشد، APK همان flavor را دریافت می‌کند.
7. برای Admin فقط `app-admin-debug.apk` و برای Viewer فقط `app-viewer-debug.apk` انتخاب می‌شود.
8. پس از پایان دانلود، نصب سیستم باز می‌شود.

## نتیجه مورد انتظار
- اگر نسخه جدید وجود نداشته باشد: هیچ APK جدیدی دانلود نمی‌شود و پیام «برنامه شما به‌روز است» نمایش داده می‌شود.
- اگر نسخه جدید وجود داشته باشد: نسخه جدید و شماره Build نمایش داده می‌شود، دانلود با درصد پیشرفت انجام می‌شود و سپس نصب آغاز می‌شود.
- فایل `update.json` محلی برای تصمیم‌گیری استفاده نمی‌شود.
- نسخه Manager نباید روی Viewer و نسخه Viewer نباید روی Manager پیشنهاد شود.

## نکته تست
برای اینکه تست واقعاً «بروزرسانی» باشد، نسخه قدیمی باید روی گوشی باقی بماند و فقط Release جدید در GitHub ایجاد شود. نصب APK جدید قبل از زدن دکمه بروزرسانی، اختلاف نسخه را از بین می‌برد.

## بررسی سریع Release
Workflow باید این دو asset را در Release ایجاد کند:
- `app-admin-debug.apk`
- `app-viewer-debug.apk`

Tag هر Release به شکل `apk-build-N` است و `N` همان `GITHUB_RUN_NUMBER` است.

# Tazieh Android ACCESS — Stage43

## قابلیت جدید: سابقه و بازگردانی سیاست هر کاربر خاص

بر پایه Stage42، بدون حذف قابلیت‌های قبلی.

### تغییرات واقعی
- قبل از هر ویرایش موفق یک کاربر خاص، نسخه قبلی پرونده در حافظه داخلی Admin ثبت می‌شود.
- برای هر شناسه نصب، حداکثر 20 نسخه قبلی نگهداری می‌شود.
- از داخل «پرونده کاربر» دکمه «سوابق سیاست» اضافه شده است.
- هر سابقه شامل زمان ثبت نسخه، پروفایل، وضعیت، تاریخ انقضا و تعداد مجوزهای فعال است.
- امکان «بازگردانی این نسخه» وجود دارد.
- بازگردانی به عنوان یک تغییر جدید ثبت می‌شود و نسخه سیاست مقصد دوباره افزایش می‌یابد؛ بنابراین مکانیزم ضد Replay Stage42 دور زده نمی‌شود.
- بازگردانی خودکار به Viewer ارسال نمی‌شود؛ پس از بازگردانی باید «ارسال سیاست» انجام شود.
- سوابق در فایل داخلی `viewer_access_special_users_history.json` نگهداری می‌شوند و خارج از Viewer هستند.

### تست ساختاری
- تعادل آکولادها و پرانتزها بررسی شد.
- ZIP پس از ایجاد با `unzip -t` بررسی می‌شود.
- Android SDK/Gradle Wrapper در محیط فعلی موجود نیست؛ بنابراین APK Build ادعا نشده است.

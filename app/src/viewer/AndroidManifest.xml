<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Viewer uses internet only for manual/optional app update checks. Normal reading remains offline. -->
    <application>
        <receiver android:name="com.example.bookapp.PolicyChangedReceiver" android:exported="true">
            <intent-filter>
                <action android:name="com.example.bookapp.POLICY_CHANGED" />
            </intent-filter>
        </receiver>
        <activity android:name=".MainActivity" android:exported="true" android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/json" />
            </intent-filter>
        </activity>
    </application>
</manifest>

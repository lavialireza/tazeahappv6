# Stage 8 — اصلاحات واقعی محتوا و دسترسی

این نسخه فقط تغییر شماره نسخه نیست. اصلاحات کدنویسی شده:

1. معرفی برنامه: آدرس مخزن GitHub حذف شده و هیچ آدرس ساختگی جایگزین نشده است.
2. پاورقی: ذخیره/ویرایش/حذف در Room حفظ شده و فرم ذخیره دیگر در حالت خالی بی‌صدا بسته نمی‌شود؛ خطای ورودی را نشان می‌دهد. دسترسی «پاورقی» نیز در مدیریت دسترسی Viewer قابل کنترل است.
3. گالری: برای هر تعزیه/مجلس یک کارت واضح «گالری تصاویر این مجلس» در فهرست همان تعزیه اضافه شده و از همانجا ورود به انتخاب عکس و مدیریت تصاویر انجام می‌شود. در Viewer فقط مشاهده فعال است.
4. کاربران خاص: شناسه قبل از ذخیره نرمال‌سازی می‌شود، ذخیره با commit انجام می‌شود، بلافاصله از SharedPreferences دوباره خوانده و راستی‌آزمایی می‌شود و خطای ذخیره در خود صفحه نمایش داده می‌شود.
5. مدیریت دسترسی: کلیدهای «پاورقی» و «معرفی برنامه» در permissionLabels و تبادل سیاست قرار دارند؛ گالری نیز مانند قبل دسترسی مستقل دارد.
6. یک ایراد ظاهری تکرار شرط‌ها در MainMenu نیز پاکسازی شد.

## وضعیت تست
- صحت ساختار ZIP با `unzip -t` قابل بررسی است.
- در این محیط Android SDK/Gradle کامل برای ساخت APK موجود نیست؛ بنابراین موفقیت build APK ادعا نمی‌شود. برای build واقعی از GitHub Actions پروژه استفاده شود.

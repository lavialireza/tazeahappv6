# Stage 42 — نسخه مستقل سیاست برای هر کاربر/دستگاه

این مرحله بر پایه Stage 41 ساخته شده و فقط لایه نسخه‌بندی سیاست دسترسی را تقویت می‌کند.

## تغییرات واقعی
- نسخه سیاست برای هر `installationId` به‌صورت مستقل نگهداری می‌شود.
- سیاست عمومی با کلید مستقل `*` نسخه‌بندی می‌شود.
- در نتیجه دریافت سیاست کاربر A دیگر باعث رد شدن یک سیاست معتبر برای کاربر B نمی‌شود.
- آخرین نسخه واردشده نیز برای هر مقصد جداگانه ثبت می‌شود.
- نسخه‌های قبلی Stage 41 برای سازگاری، در صورت نبود نقشه نسخه‌ها قابل استفاده هستند.
- هنگام ایجاد/ویرایش/حذف کاربر خاص، نسخه همان مقصد افزایش می‌یابد.
- ساخت فایل سیاست اکنون نسخه مخصوص همان مقصد را داخل فایل قرار می‌دهد.

## امنیت
اعتبارسنجی HMAC، کنترل شناسه مقصد، تاریخ انقضا و جلوگیری از Replay مرحله 41 حفظ شده‌اند.
این مرحله ادعا نمی‌کند HMAC با راز داخل APK یک DRM مطلق است؛ برای حفاظت قوی‌تر باید در آینده معماری کلید ارتقا یابد.

## تست پیشنهادی GitHub Actions
1. کاربر A با شناسه A را بسازید و سیاست نسخه جدید بفرستید.
2. کاربر B با شناسه B را بسازید و سیاست آن را بفرستید.
3. یک نسخه پایین‌تر برای A نباید پذیرفته شود.
4. همان نسخه پایین‌تر برای B، در صورت بالاتر بودن از آخرین نسخه B، نباید به‌خاطر نسخه A رد شود.
5. Replay همان فایل برای همان مقصد باید رد شود.
6. فایل با شناسه مقصد اشتباه و فایل دستکاری‌شده باید رد شود.

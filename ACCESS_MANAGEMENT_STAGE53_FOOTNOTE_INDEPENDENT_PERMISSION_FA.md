# Stage53 — مستقل‌سازی دسترسی پاورقی و انتقال به دیکشنری

## مبنا
Stage52.1 Build Fix.

## تغییرات واقعی
- Permission جدید `footnoteSync` با عنوان «انتقال پاورقی به دیکشنری» اضافه شد.
- `footnotes` همچنان مستقل است و فقط کنترل خود قابلیت پاورقی را انجام می‌دهد.
- گروه «امکانات پژوهشی» اکنون شامل `footnotes` و `footnoteSync` است.
- اگر `footnotes=true` و `footnoteSync=false` باشد، پاورقی‌ها قابل مشاهده/افزودن/ویرایش/حذف هستند اما دکمه انتقال واژه به دیکشنری نمایش داده نمی‌شود.
- علاوه بر مخفی شدن دکمه، callback نیز دوباره Permission را بررسی می‌کند تا دور زدن UI باعث افزودن به دیکشنری نشود.
- همین Permission در TextPagerScreen نیز اعمال شده است.
- مقدار پیش‌فرض `footnoteSync` مانند سایر قابلیت‌های عادی فعال است تا رفتار نسخه‌های قبلی بدون تغییر باقی بماند؛ Admin می‌تواند آن را جداگانه خاموش کند.

## نکته درباره مشکل اعمال دسترسی
در زنجیره Policy، کلیدهای Permission از `ViewerAccessPolicy.permissionLabels` در JSON تولید و در Viewer دریافت می‌شوند و `getEffectivePermissions()` آنها را به UI می‌دهد. این Stage یک Permission مستقل واقعی اضافه می‌کند، اما تأیید نهایی اعمال خاموش/روشن شدن روی دستگاه باید با Build و اجرای واقعی Admin و Viewer و ارسال Policy انجام شود.

## وضعیت Build
Android SDK/Gradle قابل اجرای کامل در این محیط موجود نیست؛ بنابراین Build موفق ادعا نشده است.

# Stage 62 — تکمیل نهایی بروزرسانی داخل برنامه

این مرحله فقط بخش بروزرسانی را تکمیل می‌کند و به سایر قسمت‌های برنامه دست نمی‌زند.

## موارد تکمیل‌شده

- سرور بروزرسانی Admin و User همچنان کاملاً جداست.
- نام پکیج واقعی هر دو Flavor در کد بررسی می‌شود:
  - Admin: `com.example.bookapp`
  - User: `com.example.bookapp.viewer`
- `access` سرور با نوع APK تطبیق داده می‌شود.
- فقط HTTPS پذیرفته می‌شود؛ حتی Redirect به HTTP هم رد می‌شود.
- `versionCode` باید معتبر و بزرگ‌تر از نسخه نصب‌شده باشد؛ Downgrade انجام نمی‌شود.
- مقدار `sha256` در `update.json` اجباری است.
- پس از دانلود، SHA-256 واقعی APK محاسبه و با سرور مقایسه می‌شود.
- APK فقط در صورت تطابق SHA-256 و Package Name و Version Code برای نصب باز می‌شود.
- فایل ناقص `.part` هرگز قابل نصب نیست.
- حجم APK بروزرسانی به 300MB محدود شده است.
- کنترل خطای HTTP، اتصال امن، فایل ناقص و APK نامعتبر انجام می‌شود.
- برنامه در حالت عادی اینترنت استفاده نمی‌کند و فقط با دکمه «بررسی بروزرسانی» به سرور Flavor خودش وصل می‌شود.

## نکته مهم

SHA-256 صحت فایل را کنترل می‌کند؛ امنیت مسیر نیز با HTTPS انجام می‌شود. APK جدید باید با همان کلید امضایی ساخته شده باشد که نسخه نصب‌شده با آن امضا شده است، وگرنه خود Android نصب را رد می‌کند.

## قالب update.json

برای هر نسخه جدید، مقدار `sha256` را با SHA-256 همان فایل APK قرار دهید. مقدار `versionCode` نیز باید از نسخه نصب‌شده بزرگ‌تر باشد.

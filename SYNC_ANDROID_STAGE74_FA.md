# Stage 74 — اتصال واقعی Android به Sync Server وب

این مرحله اتصال نسخه Android مرجع Stage 62 را به Sync Server نسخه Web اضافه می‌کند.

## امکانات
- تنظیم آدرس Sync Server از داخل تنظیمات نسخه مدیر.
- نمایش شناسه دستگاه Android.
- بررسی اتصال به `/api/health`.
- دریافت وضعیت از `/api/state`.
- تبدیل محتوای سرور به ساختار سلسله‌مراتبی Android با UID پایدار.
- ادغام محتوای دریافت‌شده با Room.
- ارسال محتوای فعلی Android به همان `/api/state` بدون حذف داده‌های غیرمحتوایی موجود در سرور.
- حالت آفلاین حفظ شده است؛ اتصال فقط با زدن «همگام‌سازی اکنون» انجام می‌شود.
- Viewer ابزار مدیریت/ارسال محتوا را ندارد.

## راه‌اندازی
1. روی لپ‌تاپ Sync Server مرحله Web را اجرا کنید؛ پیش‌فرض پورت `8091` است.
2. IP لپ‌تاپ را در شبکه محلی پیدا کنید، مثلاً `192.168.1.10`.
3. در Android مدیر → تنظیمات → «همگام‌سازی با لپ‌تاپ / Web» آدرس زیر را وارد کنید:
   `http://192.168.1.10:8091`
4. «ذخیره آدرس» و سپس «همگام‌سازی اکنون» را بزنید.

## نکته امنیتی
برای شبکه محلی آزمایشی HTTP فعال شده است. برای استفاده روی اینترنت عمومی، Sync Server باید پشت HTTPS قرار گیرد.

## محدوده این مرحله
این مرحله ابتدا **محتوای اصلی** (زمینه، تعزیه، نقش، بخش و متن) را متصل می‌کند. همگام‌سازی کامل داده‌های شخصی مانند یادداشت، علاقه‌مندی، تاریخچه مطالعه، تصاویر و گفتگوها در مرحله بعدی بر اساس همین کانال اضافه خواهد شد.

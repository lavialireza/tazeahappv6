# اصلاح واقعی گالری اختصاصی هر تعزیه — Stage 12

این مرحله فقط روی گالری تصاویر کار می‌کند.

## تغییرات واقعی
1. در فهرست تعزیه‌ها (`TaziehCatalogScreen`) برای هر تعزیه دکمه «گالری تصاویر همین تعزیه» اضافه شد.
2. کلیک گالری مستقیماً `taziehId` همان تعزیه را به مسیر گالری می‌فرستد؛ بنابراین به عنوان یا مسیر نقش وابسته نیست.
3. در صفحه نقش‌های همان تعزیه نیز ورودی مستقیم به گالری اضافه شد.
4. مسیر گالری فقط `taziehId` را دریافت می‌کند و عنوان تعزیه را مستقیماً از دیتابیس می‌خواند؛ مشکل مسیر/عنوان فارسی دیگر باعث خراب شدن ورود به گالری نمی‌شود.
5. هنگام افزودن عکس، وجود تعزیه بررسی می‌شود، تصویر واقعاً در حافظه خصوصی برنامه کپی می‌شود، سپس رکورد با همان `taziehId` در Room ثبت می‌شود.
6. اگر کپی یا ثبت تصویر شکست بخورد، خطای واقعی در صفحه گالری نمایش داده می‌شود و شکست عملیات بی‌صدا نمی‌ماند.
7. تصاویر فقط با `getByTazieh(taziehId)` همان تعزیه بارگذاری می‌شوند.
8. فضای خصوصی `files/tazieh_images` حفظ شده است.

## فایل‌های تغییرکرده
- `app/src/main/java/com/example/bookapp/ui/AppNavigation.kt`
- `app/src/main/java/com/example/bookapp/ui/screens/TaziehCatalogScreen.kt`
- `app/src/main/java/com/example/bookapp/ui/screens/ProfessionalRoleScreen.kt`
- `app/src/main/java/com/example/bookapp/ui/screens/TaziehGalleryScreen.kt`

این محیط Android SDK/Gradle اجرایی ندارد؛ بنابراین این مرحله از نظر ساخت APK ادعای Build موفق ندارد. ZIP از نظر ساختار آرشیو بررسی شده است.

# Stage 76 — همگام‌سازی چنددستگاهی و جلوگیری از حذف داده

این مرحله بعد از موفقیت Stage 75 انجام شده است.

## تغییر اصلی
- Sync Server از ذخیره مستقیم state دریافتی به **merge سمت سرور** ارتقا یافته است.
- داده‌های شخصی دستگاه‌های مختلف به‌صورت union ادغام می‌شوند و یک دستگاه نمی‌تواند با state ناقص، داده‌های دستگاه دیگر را پاک کند.
- Viewer/User هیچ‌گاه مرجع تغییر محتوای اصلی (`fields/taziehs/roles/sections/footnotes`) نیست.
- Admin می‌تواند محتوای اصلی را بر اساس `uid/id` و timestamp ادغام کند.
- نسخه پروتکل Android به `syncVersion = 3` ارتقا یافته است.
- پورت LAN همچنان `8091` است و اتصال فقط با دستور همگام‌سازی انجام می‌شود.

## نصب
1. فایل Android این مرحله را جایگزین Stage 75 کنید.
2. فایل Web Sync Server این مرحله را نیز روی لپ‌تاپ قرار دهید.
3. `sync_server.py` را اجرا کنید.
4. Android و لپ‌تاپ را روی یک شبکه قرار دهید و آدرس `http://IP-LAPTOP:8091` را در Settings وارد کنید.

## نکته
این مرحله فایل‌های باینری بزرگ را هنوز به یک مخزن ابری منتقل نمی‌کند؛ تصویر/صوتی که به‌صورت data URL در state باشد همچنان طبق منطق Stage 75 منتقل می‌شود.

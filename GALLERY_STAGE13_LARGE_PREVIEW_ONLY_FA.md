# Gallery large preview — isolated fix

مبنای این اصلاح **Stage 12 واقعی** است و مسیرهای گالری، دکمه‌ها، ذخیره‌سازی و ناوبری دست‌کاری نشده‌اند.

تنها تغییرات:
- لمس تصویر کوچک در `TaziehGalleryScreen` پیش‌نمایش همان تصویر را باز می‌کند.
- پیش‌نمایش با `Dialog` بدون محدودیت عرض پلتفرم و با `ContentScale.Fit` نمایش داده می‌شود تا تصویر بزرگ و بدون برش دیده شود.
- دکمه بستن و Back/بیرون‌زدن از Dialog برای بستن پیش‌نمایش فعال است.
- همین قابلیت در `AllImagesGalleryScreen` نیز اضافه شده است.
- ارتفاع thumbnail از 140dp به 180dp تغییر کرده تا هدف لمس واضح‌تر باشد.

هیچ تغییر دیگری در `AppNavigation.kt` یا منطق افزودن/حذف/ذخیره تصاویر انجام نشده است.

نکته: در این محیط Android SDK/Gradle برای Build واقعی در دسترس نیست؛ بنابراین موفقیت Build ادعا نمی‌شود.

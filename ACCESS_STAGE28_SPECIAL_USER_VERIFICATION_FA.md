# Tazieh Android Access Stage 28

## هدف
نمایش و راستی‌آزمایی وضعیت دسترسی Viewer پس از ثبت شناسه کاربر خاص در Admin.

## تغییر واقعی
در صفحه تنظیمات Viewer، وقتی بخش «مجوزهای Viewer» فعال است، یک کارت «وضعیت دسترسی این Viewer» نمایش داده می‌شود و موارد زیر را از حافظه فعلی برنامه می‌خواند:
- شناسه نصب واقعی همین Viewer
- منبع دسترسی مؤثر: کاربر خاص یا پروفایل عمومی
- تعداد قابلیت‌های فعال
- پروفایل و تاریخ انقضای کاربر خاص در صورت وجود

این مرحله فقط برای قابل‌مشاهده و قابل‌تست کردن نتیجه ثبت شناسه کاربر خاص است و منطق سایر قابلیت‌ها را تغییر نمی‌دهد.

Android/Gradle در این محیط اجرا نشده است؛ بنابراین Build APK ادعا نشده است.

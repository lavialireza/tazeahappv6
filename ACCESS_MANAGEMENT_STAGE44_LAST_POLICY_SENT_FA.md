# Tazieh Android — Stage 44

## قابلیت جدید: ثبت آخرین ارسال سیاست برای هر کاربر خاص

در این مرحله، پرونده هر کاربر خاص یک فیلد پایدار `lastPolicySentAt` دارد.

- هنگام «ارسال سیاست» به Viewer، پس از موفقیت شروع Intent، زمان ارسال ثبت می‌شود.
- زمان آخرین ارسال در کارت کاربر نمایش داده می‌شود.
- در «گزارش کامل کاربر» نیز آخرین ارسال نمایش داده می‌شود.
- پشتیبان JSON این مقدار را نگه می‌دارد و بازیابی می‌کند.
- رکوردهای قدیمی که این فیلد را ندارند با مقدار صفر خوانده می‌شوند و سازگاری حفظ می‌شود.
- ثبت زمان ارسال باعث افزایش نسخه سیاست نمی‌شود؛ چون صرفاً وضعیت عملیاتی است و مجوزها تغییر نکرده‌اند.

## نکته مهم
ثبت «ارسال» به معنی ثبت زمان موفقیت شروع اشتراک‌گذاری در Admin است؛ تأیید قطعی اینکه Viewer فایل را اعمال کرده، همچنان باید از سمت Viewer/آزمون دسترسی بررسی شود.

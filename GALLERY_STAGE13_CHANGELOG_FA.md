# Stage 13 — نمایش بزرگ تصویر در گالری

این مرحله فقط برای رفع مشکل نمایش تصویر در گالری است.

تغییرات واقعی:
- با لمس تصویر بندانگشتی در `TaziehGalleryScreen`، پیش‌نمایش بزرگ همان تصویر باز می‌شود.
- تصویر بزرگ با `ContentScale.Fit` نمایش داده می‌شود تا نسبت تصویر حفظ شود و تصویر بریده نشود.
- دکمه «بستن» و لمس خارج از پنجره برای خروج از پیش‌نمایش وجود دارد.
- اندازه بندانگشتی از ۱۴۰dp به ۱۸۰dp افزایش یافته است.
- همین قابلیت برای گالری تجمیعی `AllImagesGalleryScreen` نیز اضافه شده است.
- مسیر فایل واقعی تصویر در حافظه خصوصی برنامه همان مسیر ذخیره‌شده قبلی باقی می‌ماند.

هیچ تغییر دیگری در دسترسی‌ها، ذخیره عکس، دیتابیس یا مسیر گالری انجام نشده است.
